<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plexus.personal.user.infrastructure.persistence.UserMapper">

    <resultMap id="UserResultMap" type="com.plexus.personal.user.infrastructure.persistence.UserRow">
        <id property="id" column="id" />
        <result property="username" column="username" />
        <result property="passwordHash" column="password_hash" />
        <result property="displayName" column="display_name" />
        <result property="enabled" column="enabled" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT id, username, password_hash, display_name, enabled, created_at, updated_at
        FROM users
        WHERE username = #{username}
    </select>

    <insert id="insert">
        INSERT INTO users (username, password_hash, display_name)
        VALUES (#{username}, #{passwordHash}, #{displayName})
    </insert>
</mapper>
