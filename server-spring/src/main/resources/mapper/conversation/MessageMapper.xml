<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plexus.personal.conversation.infrastructure.persistence.MessageMapper">

    <resultMap id="MessageResultMap" type="com.plexus.personal.conversation.infrastructure.persistence.MessageRow">
        <id property="id" column="id" />
        <result property="conversationId" column="conversation_id" />
        <result property="role" column="role" />
        <result property="content" column="content" />
        <result property="createdAt" column="created_at" />
    </resultMap>

    <select id="insert" resultType="long">
        INSERT INTO messages (conversation_id, role, content)
        VALUES (#{conversationId}, #{role}, #{content})
        RETURNING id
    </select>

    <select id="findByConversationId" resultMap="MessageResultMap">
        SELECT id, conversation_id, role, content, created_at
        FROM messages
        WHERE conversation_id = #{conversationId}
        ORDER BY created_at DESC, id DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>
</mapper>
